import { config } from 'dotenv';

// โหลด env **ก่อน** โมดูลอื่นถูก evaluate
//
// ต้องเป็นโมดูลแยกและถูก import เป็นบรรทัดแรกเสมอ เพราะ ESM ประเมินโมดูลที่ถูก
// import ตามลำดับที่ประกาศ และประเมินให้เสร็จก่อนตัวโค้ดในไฟล์จะเริ่มทำงาน การ
// เรียก config() ไว้กลางไฟล์ (หลังบรรทัด import) จึงสายไปแล้ว
//
// เรื่องนี้ไม่ใช่ทฤษฎี: src/lib/mongodb.ts อ่าน process.env.MONGODB_URI ที่ระดับ
// โมดูล (`const uri = process.env.MONGODB_URI`) ถ้า env ยังไม่ถูกโหลดตอนนั้น
// isMongoConfigured() จะคืน false ทั้งที่ตั้งค่าไว้แล้ว แล้ว store จะเงียบ ๆ ไป
// เขียนไฟล์ใน data/ แทน Mongo — สคริปต์รายงานว่า "สำเร็จ" ครบทุกบรรทัด แต่ข้อมูล
// ไม่ได้อยู่ในที่ที่แอปจริงอ่าน
config({ path: '.env.local' });
config();
