// สีและลำดับการวางซ้อนของแต่ละเลเยอร์บนหน้าแผนที่
//
// เลือกจากหน้าที่ของข้อมูล ไม่ใช่จากความสวย: ขอบเขตหมู่เป็นกรอบอ้างอิงจึงเป็นเส้น
// ไม่มีพื้น ส่วนแปลงที่ดินกับอาคารเป็นเนื้อหาหลักจึงมีพื้นโปร่งพอให้เห็นภาพดาวเทียม
// ข้างใต้ — บนภาพดาวเทียมสีเข้มทึบจะบังสิ่งที่คนกำลังเทียบอยู่พอดี

export type LayerStyle = {
  color: string;
  weight: number;
  fillColor?: string;
  fillOpacity: number;
  /** ยิ่งมากยิ่งอยู่บน — ขอบเขตหมู่ต้องอยู่บนสุดเพื่อให้เห็นกรอบเสมอ */
  order: number;
  /** เปิดไว้ตั้งแต่แรกไหม — เลเยอร์หนักปิดไว้ก่อนเพื่อไม่ให้หน้าเปิดช้า */
  defaultOn: boolean;
};

export const LAYER_STYLES: Record<string, LayerStyle> = {
  'zone-moobang': {
    color: '#f59e0b',
    weight: 2.5,
    fillOpacity: 0,
    order: 40,
    defaultOn: true,
  },
  road: {
    color: '#ffffff',
    weight: 1.5,
    fillOpacity: 0,
    order: 30,
    defaultOn: true,
  },
  building: {
    // ย้ายจากแดงมาเป็นม่วงตอนแปลงที่ดินเปลี่ยนเป็นแดง — สองเลเยอร์ที่ซ้อนทับกัน
    // ตลอดเวลาต้องแยกออกจากกันด้วยสายตาได้ ไม่งั้นเปิดพร้อมกันแล้วอ่านไม่ออก
    color: '#c084fc',
    weight: 0.5,
    fillColor: '#a855f7',
    fillOpacity: 0.35,
    order: 20,
    // 5,460 รูปทรง — เปิดเองได้แต่ไม่เปิดให้ตั้งแต่แรก
    defaultOn: false,
  },
  parcel: {
    color: '#ef4444',
    weight: 0.7,
    fillColor: '#ef4444',
    // โปร่งกว่าอาคารมาก เพราะแปลงที่ดินปูเต็มพื้นที่ ถ้าทึบเท่ากันจะกลายเป็นแผ่น
    // แดงบังภาพดาวเทียมที่คนกำลังเทียบอยู่
    fillOpacity: 0.1,
    order: 10,
    // 7,970 รูปทรง ~4 MB — เปิดตอนต้องใช้เท่านั้น
    defaultOn: false,
  },
  'community-forest': {
    color: '#16a34a',
    weight: 2,
    fillColor: '#22c55e',
    // โปร่งพอให้เห็นเรือนยอดไม้ในภาพดาวเทียมข้างใต้ ซึ่งเป็นสิ่งที่คนเปิดเลเยอร์นี้
    // มาเทียบว่าขอบเขตตรงกับป่าจริงไหม
    fillOpacity: 0.25,
    // ใต้ขอบเขตหมู่ (40) ที่ต้องเห็นกรอบเสมอ แต่เหนือถนน (30) — พื้นสีเขียวจะบัง
    // เส้นถนนทั้งเส้นถ้าอยู่ผิดลำดับ
    order: 35,
    defaultOn: true,
  },
  'community-forest-point': {
    color: '#15803d',
    weight: 1,
    fillColor: '#4ade80',
    // จุดต้องทึบถึงจะเห็นบนพื้นป่าที่มันอยู่ข้างใน
    fillOpacity: 0.9,
    order: 50,
    // 176 จุดรกถ้าเปิดมาพร้อมกันตั้งแต่แรก — คนที่ต้องใช้เปิดเองได้
    defaultOn: false,
  },
};

export const FALLBACK_STYLE: LayerStyle = {
  color: '#22c55e',
  weight: 1.5,
  fillColor: '#22c55e',
  fillOpacity: 0.2,
  order: 0,
  defaultOn: false,
};

export function styleOf(layerId: string): LayerStyle {
  return LAYER_STYLES[layerId] ?? FALLBACK_STYLE;
}

// ── แยกสีตามกลุ่ม ───────────────────────────────────────────────────────────

/**
 * ฟิลด์ที่ใช้แบ่งกลุ่มสีของแต่ละเลเยอร์
 *
 * ระวัง: `zone_id` ของสองเลเยอร์นี้ **คนละความหมายกัน** — ของโซนหมู่บ้านคือหมู่
 * (`Moo 1`–`Moo 11`) ส่วนของแปลงที่ดินคือโซนงานรังวัด (`01`–`16`, `4`, `9`, …)
 * สีจึงถูกไล่แยกกันต่อเลเยอร์ ห้ามเอาสองชุดมาใช้ตารางสีร่วมกันเด็ดขาด ไม่งั้น
 * "โซน 04" ของแปลงที่ดินจะได้สีเดียวกับ "Moo 4" แล้วคนอ่านจะเข้าใจว่าเป็นที่
 * เดียวกัน ทั้งที่ไม่เกี่ยวกันเลย (ดูสเปกหัวข้อ Context ข้อ 7)
 */
export const COLOR_BY: Record<string, string> = {
  'zone-moobang': 'zone_id',
  parcel: 'zone_id',
};

// จานสีสำหรับแบ่งกลุ่ม — เลือกให้ต่างกันทั้งเฉดและความสว่าง เพื่อให้แยกออกบนภาพ
// ดาวเทียมที่เขียวเข้มเป็นหลัก และยังพอแยกได้สำหรับคนตาบอดสีแดง-เขียว
export const GROUP_PALETTE = [
  '#f97316',
  '#38bdf8',
  '#facc15',
  '#a855f7',
  '#f43f5e',
  '#22d3ee',
  '#84cc16',
  '#fb923c',
  '#e879f9',
  '#2dd4bf',
  '#fbbf24',
  '#60a5fa',
];

/**
 * แผนที่ค่า → สี ของเลเยอร์หนึ่ง
 *
 * ไล่สีตามลำดับค่าที่เรียงแล้ว ไม่ใช่ตาม hash ของค่า — hash ชนกันได้ง่ายเมื่อมี
 * ค่าไม่กี่ค่า แล้วสองหมู่ที่ติดกันจะได้สีเดียวกันจนดูเหมือนเป็นผืนเดียว ส่วนการ
 * เรียงก่อนรับประกันว่าค่าไม่เกินขนาดจานสีจะได้สีคนละสีเสมอ และได้สีเดิมทุกครั้ง
 * ที่โหลดใหม่ (ตราบใดที่ชุดค่าไม่เปลี่ยน)
 */
export function buildColorGroups(values: string[]): Map<string, string> {
  const sorted = [...new Set(values)].sort((a, b) =>
    a.localeCompare(b, 'th', { numeric: true })
  );
  const out = new Map<string, string>();
  sorted.forEach((v, i) => out.set(v, GROUP_PALETTE[i % GROUP_PALETTE.length]));
  return out;
}

// สีตอนเอาเคอร์เซอร์ไปชี้ — เหลืองจาง ๆ ให้เห็นว่ากำลังจะเลือกอันไหนโดยไม่บัง
// ข้อมูลข้างใต้ และไม่ชนกับสีประจำเลเยอร์ใด (แดง ม่วง ส้ม ขาว)
export const HOVER_STYLE = {
  color: '#fde047',
  weight: 3,
  fillColor: '#fef9c3',
  fillOpacity: 0.4,
};

// ชื่อฟิลด์ที่คนอ่านรู้เรื่อง — ใช้ในป๊อปอัปตอนคลิกดูรายละเอียด ฟิลด์ที่ไม่อยู่ใน
// นี้แสดงชื่อดิบตามที่มีในไฟล์ ไม่ซ่อน เพราะการซ่อนฟิลด์ที่ไม่รู้จักทำให้ข้อมูลที่
// เพิ่งเพิ่มเข้ามาหายไปจากสายตาโดยไม่มีใครรู้
export const FIELD_LABELS: Record<string, string> = {
  zone_id: 'โซน/หมู่',
  'Area Km2': 'พื้นที่ (ตร.กม.)',
  'Area Ria': 'พื้นที่ (ไร่)',
  parcel_cod: 'รหัสแปลง',
  block_id: 'บล็อก',
  lot: 'ล็อต',
  rai: 'ไร่',
  ngan: 'งาน',
  wa: 'ตร.วา',
  subwa: 'เศษ ตร.วา',
  province: 'จังหวัด',
  amphur: 'อำเภอ',
  tambol: 'ตำบล',
  Id_Chanod: 'เลขโฉนด',
  parcel_no: 'เลขที่ดิน',
  survey_no: 'เลขที่สำรวจ',
  land_no: 'หน้าสำรวจ',
  own_Hse_no: 'บ้านเลขที่เจ้าของ',
  own_moo: 'หมู่ (เจ้าของ)',
  own_soi: 'ซอย (เจ้าของ)',
  own_villag: 'หมู่บ้าน (เจ้าของ)',
  own_road: 'ถนน (เจ้าของ)',
  own_tambol: 'ตำบล (เจ้าของ)',
  own_amphur: 'อำเภอ (เจ้าของ)',
  own_provin: 'จังหวัด (เจ้าของ)',
  name: 'ชื่อ',
  name_en: 'ชื่อ (อังกฤษ)',
  alt_name_e: 'ชื่ออื่น',
  ref: 'รหัสสายทาง',
  highway: 'ประเภทถนน',
  surface: 'ผิวทาง',
  lanes: 'ช่องจราจร',
  maxspeed: 'จำกัดความเร็ว',
  moo: 'หมู่',
  point_n: 'ลำดับหมุด',
  // "โดยประมาณ" ไม่ใช่คำถ่อมตัว — ตัวเลขนี้คำนวณจากรูปทรงที่วาดไว้ ไม่ใช่เนื้อที่
  // ตามทะเบียนของกรมป่าไม้ ถ้าเขียนลอย ๆ ว่า "พื้นที่ (ไร่)" คนจะเอาไปอ้างผิด
  area_rai: 'พื้นที่โดยประมาณ (ไร่)',
  area_km2: 'พื้นที่โดยประมาณ (ตร.กม.)',
};

export function labelOf(field: string): string {
  return FIELD_LABELS[field] ?? field;
}
